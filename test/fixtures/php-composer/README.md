# php-composer

A fixture repository modelling a PHP project whose automation is strong but
whose written instructions are thin. It exists so the automation detectors are
exercised against a Composer project.
