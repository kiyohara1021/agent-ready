{
  "name": "node-healthy",
  "version": "1.0.0",
  "description": "Fixture repository modelling a well-prepared Node.js project.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/example/node-healthy.git"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "pino": "^9.5.0"
  },
  "scripts": {
    "dev": "node --watch src/index.js",
    "test": "node --test",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  }
}
