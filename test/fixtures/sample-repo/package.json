{
  "name": "sample-repo",
  "version": "1.0.0",
  "description": "Fixture repository for agent-ready tests.",
  "license": "MIT",
  "scripts": {
    "test": "node --test",
    "lint": "eslint ."
  }
}
